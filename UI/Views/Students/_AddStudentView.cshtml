@using Interfaces.ViewModels.Student
@model IAddStudentViewModel

<form asp-action="AddStudentPostAction" role="form">
    <div class="col-md-3">
            <div class="mt-2">
            <label asp-for="FullName"></label>
                <div>
                    <input class="form-control" asp-for="FullName"/>
                </div>    
            </div>

            <div class="mt-2">
            <label asp-for="EmailAdress"></label>
                <div>
                    <input class="form-control" asp-for="EmailAdress"/>
                </div>    
            </div>

        <button type="submit" class="btn btn-success mt-2">Add Student</button>
    </div>
</form>